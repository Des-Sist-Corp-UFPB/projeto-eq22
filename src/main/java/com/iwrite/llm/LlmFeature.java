package com.iwrite.llm;

/**
 * Stable identifiers for product features that execute LLM calls.
 * Persisted by name; renaming a constant is a breaking audit change.
 */
public enum LlmFeature {
    SCENE_ANALYSIS
}
